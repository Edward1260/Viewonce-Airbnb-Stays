services:
  - type: web
    name: viewonce-airbnb-stays
    env: node
    buildCommand: npm ci && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: NODE_ENV
        value: production
    plan: starter
    autoDeploy: false
